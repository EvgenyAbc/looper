# Looper

```bash
npx create-looper-app@latest
```

Спросит имя → поставит зависимости → откроет **http://localhost:3000**.

**Деплой:** `cd looper && npm run deploy` (с npm: `NPM_OTP=123456 npm run deploy`)
